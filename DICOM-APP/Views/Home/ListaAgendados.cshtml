@model List<DICOM_APP.Models.Agendamiento>
@{
    ViewBag.Title = "ListaAgendados";
}

<h2>Lista de Agendados</h2>

<!DOCTYPE HTML>
<html>
<head>
    <link href="~/Content/Agendados.css" type="text/css" rel="stylesheet" />
    <meta charset="utf-8" />
    <title>Lista de Agendados</title>
</head>
<body>

    @*<div id="Contenido">
            <table id="table1" class="table">
                <thead>
                    <tr>
                        <th>Codigo</th>
                        <th>Nombre</th>
                        <th>Apellido</th>
                        <th>Estado</th>


                    </tr>
                </thead>
                <tbody>
                    @foreach (var persona in Model)
                    {
                        <tr>
                            <td>@persona.Codigo</td>
                            <td>@persona.Nombre</td>
                            <td>@persona.Apellido</td>
                            <td>
                                <input id="Checkbox1" type="checkbox" />
                            </td>

                        </tr>

                    }
                </tbody>

            </table>
            <div class="btn-group">
                <button id="btnActualizar" type="button" class="btn btn-outline-info btn-block">Actualizar Lista</button>

                <button id="btnAdmitir" type="button" class="btn btn-outline-warning btn-block">Admitir Paciente</button>



            </div>
        </div>*@

    @for (int i = 0; i < Model.Count; i++)
    {
        <h5>@Model[i].dato.patientName</h5>
       
        <h5>@Model[i].dato.patientID</h5>

        <h5>@Model[i].dato.patientBD</h5>

        <h5>@Model[i].dato.patientSex </h5>

        <h5>@Model[i].modalidad.modalidad </h5>

        <h5>@Model[i].modalidad.scheduledStationAET </h5>

        <h5>@Model[i].modalidad.fecha </h5>

        <h5>@Model[i].modalidad.hora </h5>

        <h5>@Model[i].modalidad.medico </h5>
    }
    @*@for (int i = 0; i < Model.Count; i++)
        {
        <h5>@Model[i].idAgendamiento</h5>
        <h5>@Model[i].idModalidad</h5>
        <h5>@Model[i].idDatos</h5>
        }*@


</body>
</html>